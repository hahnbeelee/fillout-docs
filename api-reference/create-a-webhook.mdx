---
title: Create a webhook
openapi: "POST /webhook/create"
---

Once subscribed, your webhook will receive submissions in the same format as the entries in the `responses` list from the `/submissions` endpoint.
